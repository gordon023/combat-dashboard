services:
  - type: web
    name: combat-dashboard
    env: node
    region: singapore          # or nearest
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      NODE_ENV: production
    disk:
      - name: data
        mountPath: /data
        sizeGB: 1
      - name: uploads
        mountPath: /uploads
        sizeGB: 1
